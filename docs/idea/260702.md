## 기존 기능 보완사항

1. Youtube RSS 자동화
    '''
    현재는 곡 데이터 자동 수집의 목적으로 RSS 피드백을 통해 자동 추가하는 파이프라인을 사용중이다.
    이 파이프라인을 보완하여 아래와 같이 동작하게 하려고 한다.

    [보완된 파이프라인]
    1) RSS 곡 자동 수집
    2) Cluster Analysis
    3) main 브랜치에 커및 & 푸쉬

    현재 1, 2번은 api 형태로 제작된 것이 아니라 기능 구현까지 진행된 상태이다.
    따라서 1, 2, 3번을 api화한 후, github actions에서 크론실행 시 1-2-3 순으로 api를 호출하는 식으로 파이프라인을 구성할 계획이다. 
    '''